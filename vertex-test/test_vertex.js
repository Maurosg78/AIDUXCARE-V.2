const language = require("@google-cloud/language"); const client = new language.LanguageServiceClient(); async function analyzeText() { try { console.log("Iniciando análisis de texto con Vertex AI..."); const text = "El paciente presenta síntomas de fiebre alta y dolor de cabeza."; const document = { content: text, type: "PLAIN_TEXT", language: "es" }; const [result] = await client.analyzeEntities({ document }); console.log("PASS: Análisis de entidades completado"); console.log("Entidades encontradas:"); result.entities.forEach(entity => { console.log(`- ${entity.name} (${entity.type})`); }); process.exit(0); } catch (error) { console.error("FAIL:", error); process.exit(1); } } analyzeText();

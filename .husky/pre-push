#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"
set -e

echo "🔎 Pre-push: ejecutando TypeScript y Lint…"

if command -v pnpm >/dev/null 2>&1; then
  pnpm tsc --noEmit
  echo "✅ TypeScript OK (script)"
  pnpm lint || true
else
  npm run -s tsc -- --noEmit
  echo "✅ TypeScript OK (script)"
  npm run -s lint || true
fi

#!/usr/bin/env sh
# DISABLED: Pre-commit hooks disabled for solo development workflow
# Original hook required third-party approval which is not needed for solo development
# . "$(dirname -- "$0")/_/husky.sh"
# npm run -s validate:data || exit $?
exit 0

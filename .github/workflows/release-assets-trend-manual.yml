name: release-assets-and-trend (manual)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag a evaluar (opcional). Si vacío, autodetecta.'
        required: false
        type: string

jobs:
  run-eval:
    uses: Maurosg78/AIDUXCARE-V.2/.github/workflows/release-assets-trend-run.yml@main
    with:
      tag: ${{ inputs.tag }}
    secrets: inherit

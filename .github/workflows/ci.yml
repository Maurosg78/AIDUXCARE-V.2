name: CI
name: CI
on:
on:
  pull_request:
  pull_request:
  push:
  push:
    branches: [ main, feature/* ]
    branches: [ main, feature/* ]


jobs:
jobs:
  test:
  test:
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
      - uses: pnpm/action-setup@v3
        with:
        with:
          version: 9
          version: 9
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
        with:
        with:
          node-version: 20
          node-version: 20
          cache: 'pnpm'
          cache: 'pnpm'
      - uses: actions/setup-java@v4
      - uses: actions/setup-java@v4
        with:
        with:
          distribution: 'temurin'
          distribution: 'temurin'
          java-version: '17'
          java-version: '17'
      - run: PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=true pnpm install --frozen-lockfile=false
      - run: PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=true pnpm install --frozen-lockfile=false
      - name: Install firebase-tools (global)
      - name: Install firebase-tools (global)
        run: npm i -g firebase-tools@13
        run: npm i -g firebase-tools@13
      - name: Restore Firebase emulator cache
      - name: Restore Firebase emulator cache
        id: firebase_cache
        id: firebase_cache
        uses: actions/cache/restore@v4
        uses: actions/cache/restore@v4
        with:
        with:
          path: ~/.cache/firebase/emulators
          path: ~/.cache/firebase/emulators
          key: ${{ runner.os }}-firebase-emulators-${{ hashFiles('firebase.json', 'firestore.rules') }}
          key: ${{ runner.os }}-firebase-emulators-${{ hashFiles('firebase.json', 'firestore.rules') }}
          restore-keys: |
          restore-keys: |
      ${{ runner.os }}-firebase-emulators-
      ${{ runner.os }}-firebase-emulators-
      - name: Run tests (Firestore+Auth emulators)
      - name: Run tests (Firestore+Auth emulators)
        env:
        env:
          FIREBASE_AUTH_EMULATOR_HOST: 127.0.0.1:9099
          FIREBASE_AUTH_EMULATOR_HOST: 127.0.0.1:9099
        run: pnpm test:ci
        run: pnpm test:ci


      - name: Save Firebase emulator cache  if: steps.firebase_cache.outputs.cache-hit != 'true'

        if: steps.firebase_cache.outputs.cache-hit != 'true'
  continue-on-error: true
  continue-on-error: true
  continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ~/.cache/firebase/emulators
          key: ${{ runner.os }}-firebase-emulators-${{ hashFiles('firebase.json', 'firestore.rules') }}
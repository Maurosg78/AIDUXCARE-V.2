name: Typecheck

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  typecheck:
    name: TypeScript typecheck
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      # Must match package.json "packageManager" (pnpm@…); otherwise action defaults to pnpm 9 and fails.
      - uses: pnpm/action-setup@v4
        with:
          version: 10.29.2
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Typecheck
        run: pnpm -s typecheck

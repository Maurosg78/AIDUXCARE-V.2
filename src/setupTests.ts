import "@testing-library/jest-dom"; import { vi, expect, beforeAll, afterEach } from "vitest"; global.expect = expect; global.vi = vi; const mockCrypto = { getRandomValues: (arr) => { for (let i = 0; i < arr.length; i++) { arr[i] = Math.floor(Math.random() * 256); } return arr; }, randomUUID: () => "mock-uuid-" + Math.random().toString(36).substring(2, 15) }; Object.defineProperty(global, "crypto", { value: mockCrypto, writable: true, configurable: true });

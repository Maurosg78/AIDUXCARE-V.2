export interface SOAPMetadata {
  language?: string;    // 'en-CA' | 'es-ES' | etc.
  market?: string;      // 'CA' | 'ES' | etc.
  sessionType?: string; // 'initial' | 'followup' | ...
}
export type SOAPNote = any;

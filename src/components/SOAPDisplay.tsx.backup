import React from 'react';
import { EditableSOAPNote } from '../types/soap';

interface SOAPDisplayProps {
  soapNote: EditableSOAPNote | any; // Temporal any para compatibilidad
}

export const SOAPDisplay: React.FC<SOAPDisplayProps> = ({ soapNote }) => {
  if (!soapNote) {
    return (
      <div className="p-6 text-center text-gray-500">
        No hay nota SOAP generada
      </div>
    );
  }

  return (
    <div className="p-6 space-y-6">
      <h2 className="text-2xl font-bold text-gray-900">Nota SOAP Profesional</h2>
      
      {/* SUBJECTIVE */}
      <div className="bg-blue-50 rounded-lg p-4">
        <h3 className="text-lg font-semibold text-blue-900 mb-2">SUBJETIVO</h3>
        <div className="space-y-2 text-gray-700">
          <p>{soapNote.subjective?.content || soapNote.subjective || 'Sin datos subjetivos'}</p>
        </div>
      </div>

      {/* OBJECTIVE */}
      <div className="bg-green-50 rounded-lg p-4">
        <h3 className="text-lg font-semibold text-green-900 mb-2">OBJETIVO</h3>
        <div className="space-y-2 text-gray-700">
          <p>{soapNote.objective?.content || soapNote.objective || 'Sin datos objetivos'}</p>
        </div>
      </div>

      {/* ASSESSMENT */}
      <div className="bg-yellow-50 rounded-lg p-4">
        <h3 className="text-lg font-semibold text-yellow-900 mb-2">EVALUACIÓN</h3>
        <div className="space-y-2 text-gray-700">
          <p>{soapNote.assessment?.content || soapNote.assessment || 'Sin evaluación'}</p>
        </div>
      </div>

      {/* PLAN */}
      <div className="bg-purple-50 rounded-lg p-4">
        <h3 className="text-lg font-semibold text-purple-900 mb-2">PLAN</h3>
        <div className="space-y-2 text-gray-700">
          <p>{soapNote.plan?.content || soapNote.plan || 'Sin plan de tratamiento'}</p>
        </div>
      </div>

      {/* Actions */}
      <div className="flex gap-4 pt-4 border-t">
        <button className="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
          Editar SOAP
        </button>
        <button className="px-6 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700">
          Exportar PDF
        </button>
        <button className="px-6 py-2 bg-gray-600 text-white rounded-lg hover:bg-gray-700">
          Guardar Borrador
        </button>
      </div>
    </div>
  );
};

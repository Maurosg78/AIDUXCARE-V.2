// NOTE: This component is a stub - not currently used in pilot
// CommandCenterPage.tsx (old version) references this but it's not implemented
import React from 'react';

interface SessionTypeSelectionProps {
  patientId: string;
  patientName?: string;
  onBack?: () => void;
}

export const SessionTypeSelection: React.FC<SessionTypeSelectionProps> = () => {
  return null;
};

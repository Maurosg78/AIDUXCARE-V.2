const { initializeApp } = require("firebase/app"); const { getFirestore, collection, getDocs } = require("firebase/firestore"); require("dotenv").config(); const firebaseConfig = { apiKey: process.env.VITE_FIREBASE_API_KEY, authDomain: process.env.VITE_FIREBASE_AUTH_DOMAIN, projectId: process.env.VITE_FIREBASE_PROJECT_ID, storageBucket: process.env.VITE_FIREBASE_STORAGE_BUCKET, messagingSenderId: process.env.VITE_FIREBASE_MESSAGING_SENDER_ID, appId: process.env.VITE_FIREBASE_APP_ID, measurementId: process.env.VITE_FIREBASE_MEASUREMENT_ID }; const app = initializeApp(firebaseConfig); const db = getFirestore(app); async function listProfessionals() { try { console.log("🔍 Listando todos los profesionales..."); const professionalsRef = collection(db, "professionals"); const querySnapshot = await getDocs(professionalsRef); if (querySnapshot.empty) { console.log("❌ No hay profesionales registrados"); return; } console.log(`✅ Encontrados ${querySnapshot.size} profesionales:`); querySnapshot.forEach((doc) => { const data = doc.data(); console.log(`📋 ID: ${doc.id}`); console.log(`   Nombre: ${data.fullName || "N/A"}`); console.log(`   Email: ${data.email || "N/A"}`); console.log(`   Título: ${data.professionalTitle || "N/A"}`); console.log(`   Especialidad: ${data.specialty || "N/A"}`); console.log("----------------------------------------"); }); } catch (error) { console.error("❌ Error:", error); } } listProfessionals();

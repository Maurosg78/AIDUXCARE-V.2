// REMOVE these lines:
// const mockPatients = [...];
// if (mockPatients.length > 0 && !selectedPatient) { setSelectedPatient(mockPatients[0]); }
// patients={mockPatients}
// selectedPatient={selectedPatient}
// : selectedPatient; // Fallback to mock

// REPLACE with:
// Pure transcript-based extraction with intelligent defaults
// No fake patients, no hardcoded data
// Real clinical workflow

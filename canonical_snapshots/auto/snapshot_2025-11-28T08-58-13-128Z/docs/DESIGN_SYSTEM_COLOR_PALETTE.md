# 🎨 **AIDUXCARE OFFICIAL COLOR PALETTE**

**Version:** 1.0.0  
**Status:** ✅ **OFFICIAL - USE ONLY THESE COLORS**

---

## 🎯 **PRIMARY IDENTITY COLORS**

### **Gradient (Primary)**
- **Start:** `#667EEA` (Primary Blue - Soft periwinkle)
- **End:** `#764BA2` (Primary Purple - Muted purple)
- **Usage:** Primary buttons, brand elements, headers

### **Hover States**
- **Start:** `#5A6FD8` (Primary Blue Hover)
- **End:** `#6A4190` (Primary Purple Hover)

---

## 🎨 **SEMANTIC COLORS (Based on Identity)**

### **Success (Green)**
- **Base:** `#10B981` (emerald-500)
- **Light:** `#D1FAE5` (emerald-100)
- **Dark:** `#059669` (emerald-600)
- **Usage:** Success messages, completed states

### **Warning (Amber)**
- **Base:** `#F59E0B` (amber-500)
- **Light:** `#FEF3C7` (amber-100)
- **Dark:** `#D97706` (amber-600)
- **Usage:** Warnings, pending states

### **Error (Red)**
- **Base:** `#EF4444` (red-500)
- **Light:** `#FEE2E2` (red-100)
- **Dark:** `#DC2626` (red-600)
- **Usage:** Errors, critical alerts

### **Info (Primary Blue)**
- **Base:** `#667EEA` (primary-blue)
- **Light:** `#E0E7FF` (indigo-100)
- **Dark:** `#5A6FD8` (primary-blue-hover)
- **Usage:** Information messages, info states

---

## 🚫 **FORBIDDEN COLORS**

### **DO NOT USE:**
- ❌ `bg-black` / `text-black` / `bg-slate-900` / `bg-gray-900`
- ❌ `bg-rose-*` / `text-rose-*` (use red instead)
- ❌ `bg-sky-*` / `text-sky-*` (use primary-blue instead)
- ❌ `bg-indigo-*` / `text-indigo-*` (use primary-blue instead)
- ❌ `bg-fuchsia-*` / `text-fuchsia-*` (only for logout button)
- ❌ Any color not in this palette

---

## ✅ **APPROVED COLORS**

### **Neutrals (Gray Scale)**
- `bg-gray-50` / `text-gray-50` - Backgrounds
- `bg-gray-100` / `text-gray-100` - Light backgrounds
- `bg-gray-200` / `text-gray-200` - Borders
- `bg-gray-300` / `text-gray-300` - Disabled states
- `bg-gray-500` / `text-gray-500` - Secondary text
- `bg-gray-700` / `text-gray-700` - Primary text
- `bg-gray-900` / `text-gray-900` - **FORBIDDEN** (use gray-700 instead)

### **White/Black**
- `bg-white` / `text-white` - White backgrounds/text
- `bg-black` / `text-black` - **FORBIDDEN** (use gray-700 for text, white/gray-50 for backgrounds)

---

## 🎯 **SPECIAL CASES**

### **Logout Button (Intense Gradient)**
- **Gradient:** `from-fuchsia-600 via-purple-600 to-blue-600`
- **Hover:** `from-fuchsia-700 via-purple-700 to-blue-700`
- **Usage:** Only for logout button

### **Status Indicators**
- **Success:** `bg-emerald-50 border-emerald-200 text-emerald-700`
- **Warning:** `bg-amber-50 border-amber-200 text-amber-700`
- **Error:** `bg-red-50 border-red-200 text-red-700`
- **Info:** `bg-indigo-50 border-indigo-200 text-indigo-700` (or use primary-blue variants)

---

## 📋 **REPLACEMENT GUIDE**

| Old Color | Replace With |
|-----------|--------------|
| `bg-slate-900` | `bg-gradient-to-r from-primary-blue to-primary-purple` |
| `bg-black` | `bg-gray-700` (text) or `bg-white` (backgrounds) |
| `bg-rose-*` | `bg-red-*` |
| `bg-sky-*` | `bg-indigo-*` or `bg-primary-blue` |
| `bg-indigo-*` | `bg-primary-blue` or `bg-indigo-*` (only for info states) |
| `text-black` | `text-gray-700` or `text-gray-900` (if needed) |

---

**This palette is MANDATORY. All colors must come from this palette.**


rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TEMP (pilot): allow read/write only for authenticated users.
    // Tighten later (tenant/patient scoping) once paths are finalized.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}

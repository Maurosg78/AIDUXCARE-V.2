rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS COMPLETAMENTE PERMISIVAS PARA DESARROLLO
    // Permitir TODAS las operaciones en TODOS los documentos
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 
